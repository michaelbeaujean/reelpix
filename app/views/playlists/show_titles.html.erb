<% if @response_class.instance_of?(Array) %>
  <% @response_class.each do |title| %>
    <p>Title:</p><%= title["Title"] %> <br>
    <img src="<%= title["ImageUrl11"] %>" alt="Title Image" >
    <p><%= title["Genre"] %>,  <%= title["Rating"] %> </p>
    <p><%= title["Description"] %></p>
      <!--   form to create a new title
    send back title to save
    need to connect it to a playlist -->
    <%= form_for(Title.new) do |f| %>
    <input type="text" name="title[Title]" value="<%= title["Title"] %>">
    <select name="title[playlist_id]">
      <% current_user.playlists.each do |playlist| %>
    <option value="<%= playlist.id %>"><%= playlist.name %></option>
    </select>
    <%= f.submit %>
  <% end %>
    <% end %>
  <% end %>

<% elsif @response_class.instance_of?(Hash) %>
  <p>Title:</p><%= @response_class["Title"] %> <br>
  <img src="<%= @response_class["ImageUrl11"] %>" alt="Title Image" >
  <p><%= @response_class["Genre"] %>,  <%= @response_class["Rating"] %> </p>
  <p><%= @response_class["Description"] %></p>
  <%= form_for(Title.new) do |f| %>
<!--   form to create a new title
  send back title to save
  need to connect it to a playlist -->
  <input type="text" name="@response_class[Title]" value="<%= @response_class["Title"] %>">
  <select name="title[playlist_id]">


    <% current_user.playlists.each do |playlist| %>
    <option value="<%= playlist.id %>"><%= playlist.name %></option>
    <% end %>


  </select>
    <%= f.submit %>
  <% end %>
<% end %>

<a href="/">Home</a>
<a href="/logout">Logout</a>

